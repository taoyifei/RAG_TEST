# 18288 员工试用发布门禁（2026-09-26）

**判定：`BLOCKED_BY_INTEGRATION_OR_DEPLOYMENT`。** 本轮只改动并复验 8289 候选环境；现有 `http://10.242.180.54:18288/` 和 `/kb/` 均保持原服务，未执行生产切流。54 的 8288 无监听；用户所说的正式域名实际是 54:18288。当前候选源码沿 `wanshitong-stable` 增量修复，实际 8289 镜像见下表；本文件随修复提交交付。

| 门禁 | 结果 | 本轮现场证据与剩余条件 |
|---|---|---|
| G1 两名真实用户与隔离 | **BLOCKED** | 一名真实 RDMS 用户在 8289 登录、提问并读历史；普通用户请求 `/kb/api/admin/ops/summary` 返回 401。尚无第二名真实用户，不能证明跨用户历史、引用和记录隔离；退出重登也未作为本轮独立门禁复验。须由第二名真实 RDMS 用户在独立浏览器完成登录、提问、历史与交叉访问检查，并核对登出后身份切换。 |
| G2 问答与产品行为 | **PASS（冻结资料范围）** | 真实浏览器问答有原生流式正文与 3 条引用；一段会话连续 12 轮后刷新仍显示首末回答及引用；“有帮助”反馈与同一 Trace 落库，管理员会话的反馈列表与 Trace 详情 API 现场读回 200。无资料问题正常完成且无伪引用。真实停止后落库为 `cancelled`、刷新显示“已停止回答”；真实断线刷新后点击“恢复原回答”，等待原流结束并恢复其正文和引用，库中该问题只有一条记录。引用原件 DOCX 下载成功。此结论是有限产品闭环，不代表完整内容正确率或复杂文档上传通过。 |
| G3 小范围容量与共享资源 | **PARTIAL（公共问答准入已验证）** | 2026-09-27 在 8289 网关新增单进程 FIFO 队列：最多 4 条公共问答流运行、8 条等待，等待上限 300 秒，第 13 条立即返回 HTTP 429。隔离 HTTP 集成验证观察到 4 运行、8 等待、1 拒绝；释放后 12 条均完成且原生问答调用恰为 12 次。真实 RDMS 会话分 3 轮各发起 4 条并发请求，共 12 条均返回 200、原生结束事件且无错误；候选网关、引擎、OCR 及合规 OCR 仍运行。该准入只覆盖当前单进程湾事通公共问答与续流，不限制原生后台任务、其他应用或共享模型的全部调用；正式开放前仍须核对同一模型的额外调用与共享负载。 |
| G4 配套备份和隔离恢复 | **PARTIAL** | 60 上受限目录 `backup/pilot-preflight-20260926` 保留 PostgreSQL、原生文件、网关 SQLite、配置及独立密钥备份，校验清单通过。隔离项目 `wst_pilot_restore_20260926` 使用独立卷、网络与本机端口恢复；当前 r2 镜像下读出 19 条历史、58 条引用、公共绑定，并完成原生问答，得到正文和引用。恢复端复用现有模型/OCR 服务，未验证整机灾难恢复或新 SSO origin；本次快照非跨库原子截点，尚不满足正式切流的数据一致性要求。须在切流前冻结写入并重取一致快照，随后核对清单和抽样恢复。隔离容器和专用卷已停止清理，备份保留。 |
| G5 18288 入口与 SSO | **BLOCKED** | 当前路径是 `54:18288 → 60:8288 → 60:18290`，中间代理对 `/` 有公共根别名；8289 是 `54:8289 → 60:18289 → 60:8289`，目前仅 `/kb/` 可用。候选 SSO 登记只含 54:8289，正式旧栈使用 54:18288 的另一组 client ID、验证地址与回调；直接改转发会导致登录或根入口失败。须在批准后的受控窗口核对并配置 18288 的现有合法凭据、Origin、回调、Cookie 与根别名，然后实际用原 RDMS 登录及问答烟测。旧生产会话/反馈是否迁移仍待明确，不能擅自丢弃。 |

## 本轮修正与验证边界

- 8289 用户端历史菜单浅色模式已修正配色，并移除“旧版记录”入口；旧数据库/镜像未删除。用户端没有新增试用说明文字。
- 断线恢复遇到原流仍占用会话锁时，沿同一 Trace 等待并回读原结果，避免重复生成；停止确认后上游以失败或断线结束时，历史仍记为停止。前端定向测试 52 项、构建和受影响文件 ESLint 通过；网关 store 定向测试 5 项通过。真实 8289 浏览器已复验上述两条修正。
- Windows 本机运行 `tests/wanshitong/test_sso_session.py` 与 `test_sso_api.py` 时，测试收集被缺少 `tokenizers` 阻断，未记为通过。当前 8289 网关镜像直接用合成的超长 RDMS 权限声明发出 Cookie，长度 348 字符、上限 3500、角色与权限均未进入会话，回读用户 ID 正确；一名真实用户已在 8289 登录。此项不替代 18288 正式 origin 的登录验证。
- 现有 Cookie 超限修复来自历史提交 `d27fdcc303486e5d1c3341ea2868b9d34caf11cb`，当前分支及运行镜像均包含：不把部门/角色/权限写入公共 Cookie；显示名过长时退回用户 ID。浏览器可能同时携带候选与旧生产 Cookie，正式烟测仍须检查真实 `Set-Cookie` 与登录闭环。
- 8289 运行镜像：gateway `sha256:139e90ed426bf61102df113659b2d4a8ee90c9644e50cd745219e23023b5e4cb`；edge `sha256:0b53f5e72ec7e6a19d118917a4ad563f3eb8744b69244e201e0bdcfc1998f237`。原生 WeKnora 引擎与知识范围未因本轮改动重建。旧候选及回退镜像保留。
- OPC 内容偏差、8192 token 上下文上限及含图 DOCX/OCR 质量按[外壳等价性复核](WRAPPER-EQUIVALENCE-REVIEW.md)和[此前验收](ACCEPTANCE-20260926.md)登记为已知原生组合限制；没有在湾事通外壳添加答案裁剪或规则。本轮阻塞是 G1/G3/G4/G5 的集成及部署事实。

满足 G1–G5 后，才能改判 `READY_FOR_18288_PILOT_PENDING_APPROVAL`；得到明确生产切流批准，并在正式域名完成真实登录、问答、引用烟测后，才能称为 `PILOT_LIVE`。所需影响与回退见[切流手册](PILOT-CUTOVER-ROLLBACK.md)。

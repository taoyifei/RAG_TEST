# 员工试用范围（待生产门禁通过）

试用入口保持 `http://10.242.180.54:18288/`，继续使用现有 RDMS 登录与湾事通用户界面。管理端使用同域 `/kb/admin/`，管理知识资料、模型、用户端设置、记录与诊断，不提供管理员问答。**当前尚未切流；此处是拟定范围，不是已开放承诺。** 用户端不添加试用说明文字。

- 建议首批邀请 5–10 人；这只是邀请规模。8289 已实测三轮各 4 个同时提问；公共问答网关最多 4 条运行、8 条等待，第 13 条返回 429。共享模型的后台及其他应用调用仍须按 [G3](PILOT-RELEASE-GATE.md) 核验。
- 初期冻结现有公开知识库资料，只用就绪且已发布的文档问答；现场此前记录 45 份 DOCX 解析完成，正式切流前要再次回读每份状态、启用范围、公共 Agent 与模型绑定。私有验收库、历史归档库不进入公开范围。
- 用户可提问、查看流式答案和引用、读历史、停止/恢复、提交反馈。管理员可查看同一 Trace、用户记录与诊断。新增上传先放私有库逐份核查，不在首批试用期间批量重解析或把测试样本直接传入公开库。
- PNG/扫描 PDF 的候选解析已在私有库验证。含图 DOCX 的原生 OCR 有重复文字；旧 `.doc` 的摘要曾因 8192 token 上限失败。原生回答也可能理解错误或因上下文上限失败；界面须如实显示状态和引用，不作外壳算法修复承诺。
- 若正常短问题持续失败、引用错绑、用户串历史，或共享模型/合规业务受影响，应立即暂停新试用流量，保留日志和新栈数据，按[切流与回退步骤](PILOT-CUTOVER-ROLLBACK.md)恢复原入口。反馈使用现有“有帮助/没帮助”与管理后台记录，不另建前端提示。

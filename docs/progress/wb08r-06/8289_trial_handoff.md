# WB-08R 阶段 06：8289 本人试用交接

当前状态：`READY_FOR_USER_TRIAL / AWAITING_USER_ACCEPTANCE`

记录时间：2026-09-22T20:17:18+08:00

## 试用入口

- 地址：`http://10.242.180.54:8289/kb/`
- 登录：页面会跳转至 RDMS 登录页，使用内部账号登录后返回湾事通。
- 当前页面身份：真实新会话已显示“陶逸非”。文档不记录密码、Ticket、Cookie、
  Client Secret 或完整回调查询串。
- 54 转发：`10.242.180.54:8289 -> 10.242.180.60:18289`。
- 60 测试前缀代理：`/kb` 精确剥离一次后转发到 `127.0.0.1:8289`。

## 当前候选身份

| 项目 | 值 |
| --- | --- |
| 源码 revision | `d814f8d58d89418ced99aefe470fff58ee4b6b6c` |
| 镜像 | `rag-test-wanshitong:wb08r06-d814f8d` |
| 镜像 ID | `sha256:96f1aa48eed69de4a98f48af6f4fa89ebf5b35a6392c30697ee855b105f5ecd9` |
| 应用容器 | `wanshitong-sso-candidate-app` / `healthy` |
| 前缀代理 | `wanshitong-wb08r06-prefix-proxy` / `healthy` |
| 数据目录 | `/data/tyf/wanshitong-wb08r06-d814f8d/data` |
| 产品资产 | 38 个文件，1,143,638 bytes；manifest `933c8ad4dfccaa2226047b311808ac9ddd6f30cc2c65fcf024cafa861bae1468` |

60 上的 `rag-test-wanshitong` 镜像中仅保留当前候选和原生产镜像；旧阶段 05
候选容器及镜像已删除。本轮阶段 06 的 54/60 传输归档、本地构建归档和本地
Docker 镜像均已清理。正在运行的 8289 应用、前缀代理和 54 转发保留，不属于
过期资源。

## 已通过的本人试用前检查

1. 新浏览器上下文从 8289 跳转 RDMS，真实登录成功并返回 8289。
2. 登录后显示受信用户名，刷新及测试代理替换后会话仍有效。
3. 点击推荐“固定资产折旧的起算规则是什么？”只产生一个问答，得到完整终态。
4. 新版引用显示为“引用依据（1段）”，可展开到真实制度来源。
5. 与生产相同的联合研发预算问题在当前镜像上得到简洁答案和 1 个直接来源。
6. 54 入口和生产入口分别为 HTTP 200；生产容器身份、启动时间和健康状态不变。

## 54 转发队列核对

8289 转发进程使用生命周期修正版 `tcp_forward.py`，并发上限 32、监听 backlog
100、空闲超时 120 秒、关闭超时 2 秒。检查时监听队列为 `0/100`，进程 FD 为 7。
一次 40 并发、只读首页探针全部返回 HTTP 200，耗时范围 0.297～0.391 秒；结束后
监听队列仍为 0、无残留连接、FD 仍为 7。旧问题所对应的“目标端已结束但连接槽
不释放”没有在 8289 复现。

该转发器仍是 32 槽的测试入口；若出现超过 32 条同时存活的长连接，后续连接会
短暂等待。这是容量上限，不等于当前存在连接泄漏。54 转发为进程级服务，服务器
重启后需按发布记录恢复，尚不是正式高可用入口。

## 建议本人优先查看

- 首页推荐是否像真实工作问题，点“换一换”观察主题是否重复。
- 先点“系统上线前的安全评估申请有哪些时间要求？”或任意本职相关问题。
- 查看回答是否直接、引用来源是否与结论对应。
- 如遇到不满意结果先记录问题文本；本轮按用户要求暂不要求提交真实反馈。

## 暂不执行项

| 项目 | 状态 | 原因 |
| --- | --- | --- |
| 第二账号切换 | `NOT_RUN_BY_USER_DIRECTION` | 用户要求先放一放 |
| 3～5 人真实试用 | `NOT_RUN_BY_USER_DIRECTION` | 用户明确暂缓 |
| 真实反馈提交 | `NOT_RUN_BY_USER_DIRECTION` | 用户明确暂缓，不伪造反馈 |
| 管理员 Trace 联查 | `NOT_RUN_BY_USER_DIRECTION` | 不产生真实反馈；管理员仍使用原令牌体系 |
| 8288/18288 切换 | `NOT_EXECUTED` | 尚未取得针对当前镜像的生产授权 |

当前 8289 将保持在线供本人查看，不因本轮记录提交而停止。

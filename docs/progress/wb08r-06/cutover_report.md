# WB-08R 阶段 06 生产切换记录

状态：`RELEASED_WITH_ROLLBACK_RETAINED`

记录时间：2026-09-22T21:45:28+08:00

## 结论

用户已明确批准把当前 8289 候选版本替换旧 8288 生产版本。生产现由与候选相同的
镜像 ID 提供服务，54 的 18288 转发目标保持为 60:8288；旧生产容器、镜像、
数据、日志和密钥均保留。8289 仍作为独立测试环境运行。

## 切换前检查

- 待发布镜像：
  `sha256:96f1aa48eed69de4a98f48af6f4fa89ebf5b35a6392c30697ee855b105f5ecd9`。
- 正式回调 `http://10.242.180.54:18288/kb/sso/callback` 调用 RDMS authorize
  返回 HTTP 302，确认已登记。
- 停旧生产前，60:8288 已建立连接数为 0。
- 旧容器和镜像 inspect 已写入新生产根目录的受限 `rollback/`；旧镜像另加标签
  `rag-test-wanshitong:rollback-pre-wb08r06-20260922-2114`。

## 实际执行

1. 于 `2026-09-22T21:17:13+08:00` 停止旧 `wanshitong-app`，未删除容器。
2. 从 `/data/tyf/wanshitong/data` 创建 SQLite 一致备份和非数据库文件副本，
   新工作根为 `/data/tyf/wanshitong-wb08r06-prod-20260922-2114`；两库
   `PRAGMA quick_check` 均为 `ok`。
3. 新应用 `wanshitong-wb08r06-production-app` 使用已验收镜像和独立生产工作
   副本，只在 `127.0.0.1:18288` 发布容器端口。
4. 新前缀代理 `wanshitong-wb08r06-production-prefix-proxy` 继续占用 60:8288，
   将 `/kb` 单次剥离后转发到新应用。54 上原有
   `10.242.180.54:18288 -> 10.242.180.60:8288` 进程未重启、未改目标。
5. 生产使用 `wanshitong_prod`、`production_18288` 命名空间和正式回调；
   SSO secret 只保存在新生产受限 Secret 目录，未写入本报告或 Git。

## 切换后第 1 轮发布修正

第一次外部探测发现旧链接 `/` 返回 404，而规范入口 `/kb/` 已为 200。为兼容旧
收藏链接，前缀代理增加不缓存的 `307 / -> /kb/`，其余无 `/kb` 前缀的路径仍
返回 404。使用临时重定向而不是永久重定向，以免将来整体回退旧版时浏览器保留
错误缓存。

- 部署修正 revision：`da8bb0006bd1a740b1555fb949a6c61294a10f2a`。
- 代理脚本 SHA256：
  `73e926c922adb7390310d10230bc167612537c44a0bc8b95ab72d369accd3ab7`。
- 定向测试：`tests/deployment/test_http_prefix_proxy.py`，12 passed；Ruff passed。
- 修正后 `/` 返回 307，跟随到 `/kb/` 返回 200。

## 正式 Smoke

| 项目 | 结果 |
| --- | --- |
| `/kb/`、`/kb/live`、`/kb/ready` | HTTP 200 |
| 旧根链接 `/` | HTTP 307 到 `/kb/`，跟随后 200 |
| 真实 RDMS SSO | 使用 `taoyifei` 登录成功，回跳 18288 并显示“陶逸非” |
| 推荐问题 | “产品经理在需求评审过程中需要记录哪些内容？”得到唯一终态 |
| 引用 | “引用依据（1段）”可展开，显示真实来源《产品开发全流程工作规范》 |
| 退出 | 本地 KB 会话清理成功，RDMS 登录状态不受影响 |
| 管理员边界 | `/kb/admin` 未跳 SSO，仍显示管理口令登录 |
| 管理员登录 | 原生产 Bootstrap Token 登录成功，固定 Scope 和 46 个文档可见 |
| 新生产容器 | 应用和代理均 `healthy`，restart count 均为 0 |
| 测试入口 | `http://10.242.180.54:8289/kb/live` 仍为 200 |
| 54 生产转发 | 原 PID/命令不变；检查时监听队列 0/100，无泄漏式积压 |

真实反馈、第二账号切换和 3～5 人试用继续按用户前序要求不执行。本轮没有为发布
伪造反馈，也没有运行无关压力测试。管理员概览中的 Provider 状态卡仍显示
`degraded`，但本次真实检索、重排、生成和引用链已经完成；该显示状态作为非阻塞
运维观察保留，不在生产窗口临场扩大修改。

## 当前入口

- 用户页面：`http://10.242.180.54:18288/kb/`
- 管理员页面：`http://10.242.180.54:18288/kb/admin`
- 测试页面：`http://10.242.180.54:8289/kb/`

管理员密钥、RDMS 密码、Cookie、Ticket 和 Client Secret 均不进入仓库文档。

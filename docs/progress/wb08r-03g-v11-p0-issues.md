# WB08R-03G V11：两轮真实候选失败后的 P0 清单

V11 状态为 **FAILED / PAUSED**，`merge_allowed=false`，`independent_verification_ready=false`。两份候选均已在专用 8289 完成真实 Preflight-16；第二轮仍为 0/16 非空答案，触发“两轮即停”。本清单严格区分已观察事实、代码判断和未观察字段，不把模型原始输出内容或业务正文写入 Git。

## 两份候选和停止证据

| 项目 | C1 | C2 |
|---|---|---|
| 代码 | `06e8ce2a870c7c3c226463ff74ee4840cdb9f156` | `ef0c648ad83121fd3d1f8996a6681fea90331ba4` |
| 镜像 | `rag-test-wanshitong:wb08r03g-v11-c1-06e8ce2` | `rag-test-wanshitong:wb08r03g-v11-c2-ef0c648` |
| image ID | `sha256:0e918cd433026c7868eb61caa0f05069dc840fc1b6cfdc9de2b064e35ef6c0a9` | `sha256:1aec1f0f2c959c9d4936ffe2c6cbe599a73d51935aabf95cf2c77cc5c7c4b5f7` |
| runtime tree | `48aff8c3bfd96e9c0bebc643abf664c5e70834f99ba271328bce63ec4c8bbebc` | `a63af9d62bca0cb1418bb663f40a9db5f622c9a1f83943697145c2db8f7f9480` |
| Preflight-16 | FAILED；非空答案 0/16 | FAILED；非空答案 0/16 |
| repair | 0 | 7 |

两次均产生 16/16 唯一 final。C1 的主要断点是生成包在发送前超预算；C2 已消除该断点并使一次补充可达，但主生成和补充生成重复发生响应协议失败。A～F 均未运行。

## P0-01：真实模型与当前严格 Claim 输出协议不兼容，具体无效字段未观测

**实际观察**

- N031 三次主生成和三次补充生成全部为 `GENERATION_CLAIMS_INVALID / RESPONSE_CONTRACT`，每次最终 `generated_claim_count=0`。
- F015 三次主生成和三次补充生成全部为相同响应协议失败，最终 Claim 数仍为 0。
- F034 的主生成和补充生成也重复相同失败。
- C2 已显式关闭 thinking，相关包已经发送且低于输入预算；因此不能再把这些样本归因于 thinking 未关闭、事实未发送或预算预检。

**证据边界**

安全 trace 只保留失败类别、调用次数和包身份，没有保存 Provider 原始正文。当前不能确定是无 JSON、截断、字段缺失、枚举/类型错误、引用不存在，还是 `response_format` 与 schema 组合不兼容。具体无效字段为 `NOT_OBSERVED`。

**恢复要求**

先增加不含正文的结构失败分类观测，例如 `NO_JSON_OBJECT`、`JSON_DECODE`、`SCHEMA_REQUIRED_FIELD`、`SCHEMA_TYPE`、`UNKNOWN_SUPPORT_ID`、`TRUNCATED`，并保留字段路径、错误类别、完成原因和 token usage；不得记录答案正文或业务原文。用固定包分别比较当前 schema、缩小 schema 和 Provider 支持的结构化输出模式。没有分类证据前，不再修改语义门禁。

## P0-02：补充生成已可达，但完全复制主生成的协议失败

**实际观察**

- C1 repair 为 0；C2 repair 为 7，覆盖 N031 三次、F015 三次和 F034 一次。
- 每个补充包均实际 `TRANSPORT_SENT`，且许可来源被限制在当前 Atom。
- 7 次补充没有产生一条可解析 Claim；对应请求总时延 p50/p95 达 36955.32/62659.82 ms。

**判断**

调度问题已经修复，但“同一模型、同一严格 schema、近似同一任务再调用一次”不是有效恢复机制。继续增加 retry 次数或 timeout 只会放大延迟，不能解决协议兼容性。

**恢复要求**

补充必须针对已分类的失败采用不同合同：若是 JSON/schema 失败，使用最小字段修复合同或确定性解析器；若是来源 ID 错误，仅回送允许 ID；若是语义硬失败则禁止补充。仍应保持每 Atom 最多一次，且新机制必须在固定失败回放上证明成功率提升后才能真实部署。

## P0-03：物理表格事实和发送预算已修通，不应继续回退到召回或重复展开

**实际观察**

- C1 N031/F015 的准备包分别约 8919/11653，未发送。
- C2 N031 主/补充包约 5728/5731；F015 主/补充包约 5857/5847，全部低于 6144 并实际发送。
- N031 每包保留 4 个物理事实、14 个去重来源和 11 个保护来源；F015 每包保留 4 个物理事实、12 个去重来源和 11 个保护来源。
- 本地两 Atom、四事实探针从超预算降到估算 3592/4159，四个事实及十个依赖来源均保留。

**判断**

V10 的“物理身份与 semantic target 混用”和 V11 C1 的“同一来源反复发送”在 C2 已有正向证据。当前失败发生在模型响应协议，不是召回缺失，也不是表格事实再次被预算裁掉。

**恢复要求**

保留现有唯一事实注册表、`atom_table_fact_ids` 绑定和来源正文只发送一次的设计。后续实验必须对这两个指标设回归断言，不得为简化 schema 把事实退化为孤立单元格或重新复制来源正文。

## P0-04：关系复核仍不稳定且会占用补充槽

**实际观察**

- N035 的关系复核约 26.27 秒后发生 `HTTP_TRANSPORT / TRANSIENT`，模型状态 `NOT_OBSERVED`，最终无答案。
- A001 的关系复核完成，但 4 个结果均为 contradicted / `MODEL_NOT_SUPPORTED`；原 Claim 仍因来源结构不匹配被拒。
- N035 的 repair skip 为 `SUPPLEMENT_SLOT_USED_BY_RELATION_REVIEW`。

**判断**

关闭 thinking 没有消除复核的网络/延迟不确定性。关系复核与协议修复继续共享一个全局补充槽，会让两种不同恢复目的互相排斥。

**恢复要求**

关系复核保持可选、短超时、可降级；结构协议修复与语义复核使用不同的逐 Atom 状态和预算。复核失败不得改变已确认的独立事实，也不得自动触发更长 timeout。先用 N035 固定回放验证状态隔离。

## P0-05：硬语义边界在 C2 有实际拒绝证据，不能为通过小门删除

**实际观察**

- N033 三次：`CLAIM_STAGE_UNSUPPORTED`。
- F013 三次：`CLAIM_MODALITY_MISMATCH`。
- F024：`CLAIM_STAGE_UNSUPPORTED`。
- A001：`CLAIM_SOURCE_MISMATCH`，公开为 `CLAIM_SUPPORT_NOT_OWNED`；复核亦未支持。

**判断**

这些案例已经跨过生成协议并生成 Claim，与 N031/F015/F034 的响应协议失败不同。当前证据不支持把阶段、模态或来源所有权检查降级为软信号。若其中存在误拒，需要逐例真值与具体 Claim/Atom/来源坐标证据，而不是统一删门。

**恢复要求**

保持现有硬边界。后续只对有真值裁决的具体反例调整语义映射，并同时保留跨阶段、跨模态和跨来源负例。

## P0-06：需要的是安全的真实协议回放，而不是更多合成 happy path

**实际观察**

- 与问答直接相关的离线范围共得到 242、58、73、12 和 16 项通过；静态检查通过。
- 同一代码在 8289 真实 Preflight-16 仍为 0/16，且模型原始响应因隐私与安全要求未保存。
- 当前只能定位到 `RESPONSE_CONTRACT`，不能定位 schema 字段。

**缺口**

现有测试证明服务器能构造物理事实、压缩包并调度补充，却没有真实模型输出形状的脱敏回放。继续新增服务器端合成对象测试不会揭示 Provider 实际返回什么结构。

**恢复要求**

新增安全协议观测后，先在 8289 以少量固定请求捕获“错误类别 + 字段路径 + finish reason + token usage”，制作不含正文的回放夹具。最低覆盖：N031、F015 和 F034 的主/补充响应；N035 的复核 transport 失败；一个有效结构正例。只有回放先失败、修复后通过，才允许新的真实候选。

## 冻结结论

V11 不进入第三候选，不进入独立 Verification，不运行 A～F，不发布到生产。C1/C2 代码保留并推送用于审计，不代表可部署版本。8289 已回滚到 `bd00c56` 基线；生产 54:18288 与 60:8288 未修改。

下一次工作应明确命名为“模型输出协议兼容性实验”，其边界是安全分类和最小合同验证。继续调召回、扩大输入/timeout、增加补充次数或删除硬语义门禁都不是本轮证据支持的方案。

# P0：8289 既有文档无法从来源 Artifact 重建候选索引

## 观察

2026-09-24 在 60 服务器的隔离测试中，原 `wanshitong-sso-candidate-app` 数据副本包含 46 份活动文档、61,522 个旧索引 Chunk，但原容器挂载的 `/data/blobs/sha256` 中没有文件。对副本中一份文档发起父子索引重建时，`queue_profile_rebuild` 返回 `INVALID_DOCUMENT`，原因是活动 DocumentVersion 的来源 Artifact 不存在或摘要漂移。原 8289 容器的同一目录也检查为 0 个文件。

## 影响与边界

- 该证据仅涉及原 8289 候选数据，不能据此断言 18288 生产文档也缺少原件。
- 现有 46 份文档不能仅凭这个 8289 数据副本直接重建父子索引；若要对真实语料迁移，需先找回或重新上传原件，并按版本摘要核对。
- 本次只在单独复制的数据目录中将旧文档标为删除，上传合成 MD/TXT/PPTX/XLSX/CSV 样本并完成新父子索引及问答联调。原 8289 数据和镜像未修改，测试结束后已恢复原容器。

## 后续恢复条件

在独立数据副本中取得原始文件及其 Artifact 摘要，确认 46 份文档所需来源均可读取后，再按受控 Profile 创建新 Revision；保持 18288 服务不变。不得用旧 Chunk 文本伪造来源 Artifact。

# P0：长问题生成时模型响应格式异常

## 现场证据

8289 隔离镜像源码 `04c6bc752704adfccbff3b2d8c9b298690f1fd74`、4 份原始 DOCX 的候选索引上，长问题“请详细列出研发项目从立项到验收的各阶段步骤、责任部门和审批材料，并逐条说明。”出现 3 次 `FAILED / PROVIDER_INVALID_RESPONSE`：

| Trace | 耗时 |
|---|---:|
| `trace_29e83518193817f9834cd2221ba2e085` | 75.233 秒 |
| `trace_679d5e34bd9a7aca77787ae3db1750c55` | 74.493 秒 |
| `trace_2d345b15a5bb25bbcae278f3fb8521bb` | 74.630 秒 |

这些轮次均未发布临时正文为成功答案。短问题和有来源的追问在最终 `5fce5dc` 镜像中正常发布；长问题在最终镜像中用于验证停止传播，所得 `trace_b3def18deec6fe027787bc816741444a` 为 `CANCELLED`，不能据此宣称格式异常已解决。

## 影响和边界

长问题可能以安全错误结束，用户需缩短或重试。当前证据不能唯一归因于 Qwen 服务响应、流式协议解码、超时或候选编排。V3-03 三轮修正分别处理镜像内分块器、终态 DTO、代理下停止传播；按本次约定停止继续试错。后续需用已授权的隔离环境和同一输入核对模型原始响应与解码器边界，再决定修复位置。18288 未发布本轮代码。

## V3-03R 后续证据（2026-09-25）

候选源码 `1014995`、同类四份原始 DOCX 的独立 8289 索引上，原问题运行至自然终态：62.2 秒、`finish_reason=stop`，没有再出现 `PROVIDER_INVALID_RESPONSE`，但因为 `CITATION_MISSING`，2862 字草稿未发布（`trace_72d5c06b5f1447968bd780fe86d70f3a`）。另一条非原题长问以有效引用正常发布。详细记录见 [V3-03R 接入报告](../weknora-v3-03r/INTEGRATION_REPORT.md)。

直连 Qwen 的合成长输出在旧 2048 上限、约 74 秒时真实返回 `length`；本轮将自然输出上限调至 2680，并保留截断内层错误码。不过，对本轮原题的冻结送模包只把上限调回 2048，也得到了 `stop` 和 `[DONE]`。**原 P0 的原始流缺失，旧三次失败的唯一根因仍未证实；本轮也没有得到原题可发布答案。**本 P0 保持未完全关闭，不把格式错误未复现或安全草稿当成整题通过。

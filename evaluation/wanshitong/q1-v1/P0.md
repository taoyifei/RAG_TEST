# WB08R Q1 P0：候选小批未达验收

状态：`DIAGNOSED_ONLY`。2026-09-23 按用户最新指示停止针对个别题目的修补，保留可复核证据并上传本记录。没有把该候选合并到 `wanshitong-test`，也没有部署到 18288。

## 固定输入与执行现场

- 开发基线：`wanshitong-test` 的 `aee8755c1aa7a597ce9a84725f1352d3fcab322e`；测试源码：`44454be4eb6d430a82113b6eb752b56fce11fe1e`。
- 专用 8289 测试镜像：`rag-test-wanshitong:wb08r-q1-44454be`，ID `sha256:5aeb14da81ad5406058e755266e77728c54f089cdcfe2daca93ea1e1c9efa607`。镜像标签、容器内 `SOURCE_REVISION`、产品资产自检和候选运行配置门禁一致；8289 `/live`、`/ready` 通过。
- 活动索引：`irev_0329a35ca9ea700133f7b309160118f0`，46 文档、2880 Chunk。固定题集为 [cases.ndjson](cases.ndjson)，SHA-256 `b3fd9982d05dac8297494aa815563a1b70c614a681bacbce56c176278f3a5758`，10 题逐题独立会话、每题请求一次，无模型请求重试。
- [安全观测结果](observations-44454be.safe.json)记录 10 个 Trace ID、终态、答案摘要和耗时。公开请求累计约 80.9 秒。Trace 显示 7 次 `query.interpret`、10 次查询 Embedding、10 次重排、20 次生成（含语义复核），共 47 次 Provider 调用，重试数均为 0。生成模型为 `Qwen/Qwen3-8B-AWQ`。
- 私有原始答案、G 实际请求和响应、草稿、准备包及 Trace 留在仓库外：本机 `/home/jerry/.local/state/wb08r-q1-20260923/` 与 60 上 `/data/tyf/wanshitong-wb08r-q1-44454be/evidence/`。私有归档 SHA-256 为 `16544356a9909eb2f8bea3c57e069c44fe2d9bdd10f6d90baaaaed905d1015d4`；目录与文件分别限制为 `0700`、`0600`。Git 不含正文、会话 Cookie 或密钥。

## 阻断缺陷

### P0-01：责任人和反馈时限两种顺序均拒答

`WB08R-N-060` 和 `WB08R-Q1-N060-REV` 都返回 `INSUFFICIENT_EVIDENCE`。预期来源 `dver_67491302d0823e30f274d2e9a3382d0d` 在活动索引内，生成候选也含该来源。

- 原顺序 Trace `trace_6ab5873dad4a28636ed3f072e118cce6`：原问要求“由谁确认、几天内反馈”，实际 QueryPlan 把两个 Atom 都标成 `COUNT`，目标与关系还交叉混合。这个偏差发生在 QueryPlan。后续两 Atom 的直接支持数均为 0；准备包仍给两个 Atom 提供读单元，但没有保留表格事实。生成了 2 个候选 Claim，语义复核分别判 `SEMANTIC_CONTRADICTED`、`SEMANTIC_UNKNOWN`，最终没有发布。
- 倒序 Trace `trace_e53f1441ada3cf9c020ce74189bd83d3`：QueryPlan 正确给出 `DURATION` 与 `RESPONSIBLE_PARTY`，但两个 Atom 的直接支持数仍为 0；准备包没有保留表格事实。2 个候选 Claim 同样在复核阶段分别被判矛盾、未知，最终没有发布。因此不能把两个失败都归因于 QueryPlan 类型。

### P0-02：采购应答时限引用了不同业务关系

`WB08R-N-003` 的 Trace `trace_7b5eb9d9f5177f428439f948f41571a2` 中，G 实际收到正确来源 `E1`：“发布采购文件到应答截止时间，不得少于3日”，也收到另一来源 `E5`：“澄清需在应答截止时间至少3个自然日前发布”。模型只生成并引用 `E5`，公开答案称“采购应答的截止时间至少需提前3个自然日发布澄清”。语义复核把该 Claim 判为 supported 并发布，说明现有复核认可了所引材料中的句子，却没有挡住问句关系与材料关系错配。此处真实发生的是 G 来源选择与关系校验的链式缺陷，不宜把来源固定到某一道题。

### P0-03：推荐点击保留历史后，后续答案串入前题事实

额外 UI 要求的可见行为在真实 8289 页面成立：依次点击“上线安全评估要提前多久申请？”和“研发外协费用需要先有预算吗？”，聊天框保留两个问答；再点“新问题”才清空。对应 UI Trace 为 `trace_d07183296c78835e4a2e1165e953dd89`、`trace_5a0b1ec42ccf4e7e460a6da8ae471497`。第二个回答同时加入前题的“上线前2-3个月提出安全评估需求”，与当前外协预算问题无关。当前 UI 改动复用了会话 ID；跨主题历史如何用于解读仍需通用约束和回归验证，因此不能把单纯的“聊天记录没有清空”视为完整验收。

## 其余小批结果与边界

按固定题集的最低预期，7/10 的公开终态和必要事实可接受：F037、F040、N059、短问安全评估、F039 正问得到相应事实，F034 未编造菜单，F017 对强限定问题拒答。F040 虽给出所需责任部门，仍附带“未能完整组织全部内容”的提示。此计数仅代表本批 10 题的当前观察，不表示全量准确率或生产就绪；N003 的新来源解释仍以本次来源原文复核为依据，不覆盖旧 truth。

用户明确要求不针对特定题目打补丁，故本轮没有算法修正轮次。此前的两个提交仅包含推荐记录 UI 改动、固定复查集和候选私有取证能力；UI 的跨主题问题同列 P0。因为 Q1 与 UI 尚未通过完整验收，不执行合并与开发分支删除。18288 服务只读核对容器 ID `fb65fdaeaec4` 前后一致，未替换、停止或重建。测试后 8289 已恢复原容器 `14c0f9a42c14` 和镜像 `rag-test-wanshitong:wb08r-f06-aee8755`，`/live` 返回 200；本轮候选容器已删除，服务器镜像 `sha256:5aeb14da81ad5406058e755266e77728c54f089cdcfe2daca93ea1e1c9efa607` 与本机同标签测试镜像已精确移除。私有证据保留供后续通用修复使用。

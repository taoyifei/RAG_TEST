# WB08R Q1 小批复查 P0：时限起点和来源范围仍丢失

状态：`DIAGNOSED_ONLY`，未达到合并到 `wanshitong-test` 的验收条件。2026-09-23 的本记录接续 [44454be 候选记录](P0.md)，不覆盖旧 Trace 或旧 truth。未操作 18288 生产容器。

## 固定现场与结果

- 开发分支：`codex/wb08r-q1-algorithm-debt`；本轮候选源码 SHA：`396e988f4f36cc930ffbe1be34233a457512395d`。
- 专用 8289 候选镜像：`rag-test-wanshitong:wb08r-q1-396e988`，镜像 ID `sha256:ec9b19a58c44d1ef0e468fea66fd6c4f59ab742c49d17fa5d04c6e4b995b730b`。容器与数据使用独立的 8289 路径；产品资产自检 41/41 通过。启动时新复制的两份 SQLite 文件权限为 `0644`，被 Trace 安全门拒绝；仅将新候选内两份文件修正为 `0600` 后健康检查通过，旧 8289 候选曾自动恢复，18288 未参与切换。
- 题集仍为 [cases.ndjson](cases.ndjson) 与 [manifest.json](manifest.json) 的固定 10 题；活动索引仍为 `irev_0329a35ca9ea700133f7b309160118f0`。10 题各一次独立会话请求，均有且仅有一个最终事件，均非缓存命中。累计请求时间 `79,194.03 ms`；Provider 调用 45 次，其中问题解释 6、查询 Embedding 10、重排 10、生成含复核 19，重试 0。
- 私有完整答案和 10 条安全 Trace 保存在本机 `/home/jerry/.local/state/wb08r-q1-20260923/replay-396e988/`、`evidence-396e988/`，以及 60 的 `/data/tyf/wb08r-q1-20260923/evidence-396e988/`。10 条 Trace 归档 SHA-256：`58d1631f32ffc07b6df3f835b360d28ef6d3ac878207f89d930e5885e50d5731`。仓库不保存正文、Cookie 或密钥。

按预设最低行为计为 **9/10**：F037、F040、N060 正序和倒序、N059、安全评估短问、F039 正问均给出了相应来源事实；F034 未编造菜单；F017 只达到允许的限答边界，拒绝发布与提问等级不同的表格行，仍未完整回答。**N003 未达 `ANSWER_WITH_SCOPE`**，因此不能将本轮记作通过。此计数只对应这 10 题和该候选，不代表全库准确率。相关 Python 测试 120 项、Ruff、局部 mypy 与 `git diff --check` 通过。

## P0-04：已发送正确时限原文，发布时仍丢起点与适用范围

`WB08R-N-003` 问“采购应答要留几天？”，固定预期要求说明直接采购项目中“发布采购文件到应答截止时间，不得少于 3 日”。本轮 Trace `trace_b5c06eb088e8303bc66ff351a18d53a0` 非缓存命中，来源 S1 是《湾区研究院直接采购项目方案决策通过后实施流程V1.0》的逐字句子：“发布采购文件到应答截止时间，不得少于3日。”

公开答案却是“采购应答的截止时间不得少于3日。 [S1]”。它没有说清从**发布采购文件**起算，也没有在答案正文限定**直接采购项目**；“截止时间不得少于3日”本身不能完整表达一个区间。Trace 显示同一 Claim 的 `source_support=supported`、`question_relevance=answered`、`qualifier_fidelity=faithful`，`render_origin=model_text`，`final_coverage_by_atom.A1=ANSWERED`。这说明原文已经进入本次证据并被引用，缺口在答案关系保持和最终复核，不能归为 Chunk 丢失，也不能靠给 N003 写题面规则解决。

这里的固定期望为 `SOURCE_DERIVED_PROVISIONAL`，不冒充旧人工 truth；但本次来源原文与公开答复的起点差异可直接复核。需要通用的时限起点、终点和范围保持合同，并用相邻时限和无依据控制题验证，之后重新跑同一固定集。

## 已闭合的对照与剩余边界

- N060 正序 `trace_adeb78a79ca617e7590ed6557df57a21`、倒序 `trace_27dfb9719afed4ea8a585d137361eaeb` 各有 2 个 Atom，首次生成包各保留 3 个物理表格事实，最终各 Atom 为 `ANSWERED`。单问“业务团队收到设计文档后须在几个工作日内反馈？”的 `trace_5121aaccda18364161fe831851149465` 为 1 个 Atom、首次包 3 个物理事实、非缓存命中，回答“收到设计文档后须在2个工作日内反馈”。三组对照支持本轮不重切该文档。
- F017 在上个候选把Ⅱ级问题错误引用到同表Ⅰ级的“10分钟”行。本轮加入通用的物理表格行等级一致性校验；Trace `trace_231533d285321262722761ac2f02a6dc` 有 2 次 `QUESTION_TABLE_ROW_LEVEL_CONFLICT`，最终拒绝发布，未把Ⅰ级时限冒充Ⅱ级时限。它仍未给出完整的Ⅱ级分阶段答案，不能把安全拒答称为已解答。
- 用户要求的“你可能想问”聊天记录保留与“新问题”清空，已在前一候选真实 8289 页面按连续两问和新问题点击验证；本轮只修改 Python 表格等级校验，前端源码未变。对应前端 41 项测试此前通过。

按用户要求，停止以单题规则继续修补，记录 P0 并上传开发分支。由于固定验收未通过，不合并或删除开发分支，也不部署到 18288。

## 清理与服务核对

取证后，8289 已恢复原专用容器 `14c0f9a42c1479ef9c272ef4ff9f0c352c3855d292e9a5c9821f3021c0fdb591`，镜像 `rag-test-wanshitong:wb08r-f06-aee8755`，健康检查通过。18288 生产容器 `fb65fdaeaec49c161bc6a7e73c9af7eca01cb9c5858341c073c75a64d64abf47` 前后身份一致且健康。服务器上本轮 7 个 `wb08r-q1-*` 候选容器、7 个对应镜像及 7 份镜像传输归档已按身份精确移除；本机对应 7 个 Docker 镜像、17 份镜像/源码归档及 7 个临时构建目录已移除。私有 Trace、回放与诊断材料保留在上述受限目录供 P0 复核。

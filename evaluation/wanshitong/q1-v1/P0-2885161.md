# WB08R Q1 P0：F017 完整报送方式未进入有界生成证据

状态：`DIAGNOSED_ONLY`。本轮未达到正确完整回答的验收条件，不合并到 `wanshitong-test`，不删除开发分支，不部署到 18288。本记录接续 [上一轮 P0](P0-396e988.md)，不把端点返回的 `ANSWERABLE` 当作完整答对。

## 范围与已执行结果

- 开发分支：`codex/wb08r-q1-algorithm-debt`，从 `wanshitong-test` 的 `aee8755` 独立开发；最终实测源码为 `2885161477291790502b1f350bd26f0d011d7ab6`，活动索引为 `irev_0329a35ca9ea700133f7b309160118f0`。
- 候选只在专用 8289 上运行。`adff808` 实现来源时间事实保留、四格对照与复核器小基准；随后针对代码与方案一致后出现的 F017 问题，依次执行 `bff03f2`、`97c4e77`、`2885161` 三轮通用表格证据补全修正。三轮后仍未完整回答，按用户上限停止修改算法。没有按 F017 题号、固定答案或业务同义词表加规则。
- `bff03f2` 的固定 10 题真实回放中，N003 保留“发布采购文件到应答截止”的起点与直接采购范围，N060 正序、倒序保留表格事实；F034 未编造无依据菜单；F017 拒答，未达到完整回答。其余题的 `ANSWERABLE` 标签不在本记录中冒充逐题人工正确性判定。此后两轮仅针对仍失败的 F017 复测，没有做无关测试。
- `2885161` 的 F017 专项回放 Trace 为 `trace_0583be0fb319e066b5be8c120235f6f8`。公开正文只给出Ⅱ级“30 分钟”，并提示“本次未能完整组织全部内容”；未回答“疑似事件”和“事件确认后”两阶段分别如何报送。Trace 内部 `A1=NOT_ANSWERED`、`A2=ANSWERED`，最终路径为 `LIMITED_ANSWER`。回放终态却显示 `ANSWERABLE`，故仅看终态会高估质量。

## P0 证据定位

原始 DOCX 对应的活动索引并非完全没有报送方式。对同一表格行的离线只读核对，在全部 8 个相关 Chunk 中恢复 15 个可引证片段、2 个物理表格事实；Ⅱ级“报送方式”是从上一行纵向合并来的 101 字单元格，9 个片段可完整拼接，同时存在Ⅱ级“报送时限=30分钟”事实。这证明完整原文在活动索引里，但不证明真实问答已收到它。

真实 F017 Trace 给出的边界更窄：重排候选 12 个，邻居扩展后 23 个，规范表格闭合后有界阅读候选 31 个；其中始终没有 `chunk_7b916e1e3d313d5f6501d9de3b8f1978` 和 `chunk_f75cd1bb30d1b2656c2e28166062f25e`。前者包含“事件确认后”的关键片段。生成请求虽有 44 条 Evidence、37 个阅读单元，却只形成“Ⅱ级、报送时限、30分钟”这 1 个物理表格事实。报送方式仍是分散的原文片段，没有形成完整的纵向合并事实。

首次生成的报送方式 Claim 引入了无关的预算编制、审核内容，被语义门以 `SEMANTIC_CONTRADICTED` 拦下；时限 Claim 通过并发布。错误内容被拦截是必要保护，但这次没有补齐应答事实。**当前最窄的已证实缺口在活动索引到有界生成证据的完整依赖恢复；不能仅归因于分块丢失，也不能把模型误答视为唯一根因。** 邻居扩展与表格闭合分别记录了 `TABLE_HEADER_MISSING`、`TABLE_IDENTITY_UNDETERMINED`，但本次证据尚不足以断言哪一个具体谓词单独造成两个 Chunk 缺席。

原始 Trace、发送包与回放正文保存在本机受限目录 `/home/jerry/.local/state/wb08r-q1-20260924/`；其中 `trace-288-f017.private.json` 的 SHA-256 为 `3dcedbf25d0163c1c8d1b352c9ea7e33fe0bb249b61c71d232a07aed4e82c259`。仓库不上传原文、Cookie、密钥或私有完整 Trace。

## 与收敛方案的对照

N003 在同一已发送证据上的 A/B/C/D 四格对照只覆盖一个暂定 truth 的事实族：精简阅读 A/C 均丢失时间起点或范围；现有控制 B 给出准确但不完整的限答；完整路径 D 在这次回放中给出正确完整答复。它只能说明这一事实族的方向，不能据此宣布某条路径普遍优于另一条。独立复核小基准包含 1 个正确答案与 5 个最小错误变体；真实复核误放行 3 个错误、拦下 2 个错误，没有观察到该正确答案被误拒。样本太小且同属一个事实族，不能当作全库错误率。

本轮代码实现了来源事实保持、发送包留证和有限的四格对照，N003 的具体丢失已修复；但 F017 的有界证据仍不充分，盲测事实族与质量验收尚未完成。因此**不能确认代码已完整达到《算法反复修复的根因与收敛方案》的目标**。下一轮若重新启动，先用不含题号硬编码的结构覆盖指标检查“实际送模材料是否包含完整依赖”，再按事实族扩展盲测与复核器校准；这不是本轮继续修改或合并的授权。

## 验证和环境收尾

相关 Python 测试 82 项通过，修改范围的 Ruff、mypy 与 `git diff --check` 通过；没有用这些离线结果替代真实问答质量。前端“你可能想问”连续点击保留记录、点“新问题”才清空的功能在前一候选已验证，本轮未修改前端。未跑与本次修复无关的测试。

最终候选退出后，8289 已恢复原专用基线容器 `14c0f9a42c1479ef9c272ef4ff9f0c352c3855d292e9a5c9821f3021c0fdb591`，镜像 `rag-test-wanshitong:wb08r-f06-aee8755`，状态 healthy。18288 的原容器 `fb65fdaeaec49c161bc6a7e73c9af7eca01cb9c5858341c073c75a64d64abf47` 和原镜像 `rag-test-wanshitong:wb08r06-d814f8d` 身份未变，状态 healthy。本轮服务器上的 4 个已停候选容器、4 个候选镜像与其临时数据/传输目录已按身份清理；本机对应 4 个镜像、4 个构建目录、4 份镜像归档和临时源码归档已清理。受限目录中的必要回放与诊断证据保留供复核。

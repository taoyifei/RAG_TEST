# WeKnora V3-02 查询与材料适配接入记录

日期：2026-09-24。实施基线为 `codex/wb08r-q1-algorithm-debt@3c9b7d27d93f199cb42df61e3761a1b940a84700`，包含本次开始前的湾事通生产 SSO 历史。本轮在独立 worktree `codex/wb08r-weknora-v3-02` 实施。固定上游为 `Tencent/WeKnora@1edcd54b43606d9079bb36650efe3f68707a79ea`，具体映射见 [UPSTREAM_PORTS.md](UPSTREAM_PORTS.md)。

## 接入范围

- 候选 `wk-standard-v1` 与 `wk-standard-pc-v1` 加入一次会话查询理解，保留原问检索；改写失败回退且不能删掉本轮明确数值、否定或《文档名》。检索仍走本方 Hybrid 与独立内网 Reranker。
- 排序视图使用标题和查询附近的连续内容，不覆盖可引用原文。材料选择先尝试完整父级，超预算时依真实同版本来源区间构造局部材料，继续补位后续候选；Trace 记录实际送模别名、区间、预算和排除原因。
- 自然问答模型适配器与材料打包使用同一 8192 上下文预算。实测长材料回答因 1536 输出 token 截断后，将候选输出上限调为 2048；输入上限仍是 5000、安全余量 512。旧 grounded 路径预算未改。
- 引用缺失和非法别名分开标记：有材料但无有效引用时保留管理员草稿，不向普通用户宣称已形成可引用答案。候选错误事件只附加安全的 Provider 原因码。
- 修正父子分块跨子块重复列表编号的报告计数；部署守护仅允许 8289 独立数据目录显式启用 `parent-child`，旧默认仍为 `legacy`。回放脚本支持修改前候选协议，便于同索引对照。

本轮未接入正常湾事通页面和真实增量 SSE；这属于 V3-03。没有修改 18288 服务、生产镜像、挂载或生产活动索引。

## 隔离真实验证

60 服务器上使用 8289 的短期候选容器和独立数据目录。7 份真实原始 DOCX 上传、解析、WeKnora 父子分块、索引均成功，文档状态均为可检索；先配置内网模型再入库，配置的两次输出逐字节一致。首次实测暴露父子块报告将一个列表节点重复计数的问题；本地原始 DOCX 复现和合成列表回归通过后，7 份原件在修正镜像上全部完成 Job。旧 8289 容器及其原数据始终保留用于回滚。

修改前链与本轮首次候选在相同 7 文档、相同父子分块活动索引 `irev_d63d34a9d399691fe201f6e73bd95d42`、相同模型配置上各串行复跑 16 题。两边机器终态均为 13 个发布答案、2 个无答案处理、1 个失败，但失败问题不同；不能把机器 `ANSWERED` 计作人工准确率。首次候选的长材料失败已被定位为 `CHAT_OUTPUT_TRUNCATED`，最终镜像复测原题为 4 个有效引用、正常 `stop`。最终镜像再复测 5 个代表问题，4 个有效引用答案、1 个无答案草稿，均无接口错误。逐题情况见 [QUALITY_OBSERVATIONS.md](QUALITY_OBSERVATIONS.md)。

最终实测应用镜像 ID：`sha256:c634ca443556efed409efed3c9ed25dd76a728b8a7d07ed3352e56a5ec647039`，源码标签 `7227729057823e73381015df10a96423422f3391`。镜像资源自检通过 43 个文件。最终 Trace `trace_efe5f05a69ca4255b4144916bc808659` 的追问走了原问与改写两组通道，显示真实 Embedding、Reranker、Qwen 调用；没有旧 Atom、字段解析和逐 Claim 阶段。6 个最终复测结果与模型诊断来自 8289 隔离环境，不代表 18288 已发布。

## 本地门禁与限制

- 实现分支相关 Python 定向测试 **66 passed**，合入 Q1 后扩展的相关测试 **88 passed**；Ruff、格式检查、mypy、部署脚本 `bash -n`、`git diff --check` 通过。前端代码没有本轮改动，首次构建 `npm run build` 已通过；Go 代码未改，固定二进制 SHA-256 核对为 `491a0bd01577ecff835b0e2c93112e141f550e7bd07fabc523b98f8433e75f6f`，未重复编译 Go。
- Qwen 流未给出实际 `prompt_tokens`，输入 5000 的门禁是与适配器一致的保守估算；不能宣称已精确测得模型上下文利用率。没有进行并发容量测试或全仓测试。GitHub API 查询合并提交的 check-runs 数量为 0，没有 CI 通过证据。
- 16 题未逐条人工对照原件判定事实正确性；没有为旧阻塞题或单个文件加业务特判。模板目录仅用于导航，原始表单正文按既有策略不索引。
- 8289 的 13 个旧派生模板版本仍缺原始来源身份，本轮未写回。V3-03 正常页面、多轮会话的服务端状态、真实流式及后续 PDF/图片范围未执行。

## 清理与交付状态

真实验证结束后，已删除本轮独立修订集合 `irev_d63d34a9d399691fe201f6e73bd95d42`（删除前 111 个向量、无 alias）。按精确身份移除了本轮 7 个临时容器、5 个镜像标签和 11 个 `/data/tyf/wanshitong-wb08r-v3-02-*` 临时目录，并恢复原 8289 容器名 `wanshitong-sso-candidate-app`。恢复后容器 ID 仍是 `14c0f9a42c1479ef9c272ef4ff9f0c352c3855d292e9a5c9821f3021c0fdb591`，镜像 ID 仍是 `sha256:ca38110829dd5a1e65589e9571744a1c7342b3b2fc95e94004ba9f7aa9168d06`，状态 healthy，`/kb/` 返回 HTTP 200。

18288 生产容器 ID `fb65fdaeaec49c161bc6a7e73c9af7eca01cb9c5858341c073c75a64d64abf47`、镜像 ID `sha256:96f1aa48eed69de4a98f48af6f4fa89ebf5b35a6392c30697ee855b105f5ecd9` 未变，状态 healthy，`/kb/` 与 `/` 均返回 HTTP 200。此次只把代码合入 Q1，生产发布须另行授权。

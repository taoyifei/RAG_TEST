# WeKnora V3-00 / V3-01 阶段记录

日期：2026-09-24。实施起点：`codex/wb08r-q1-algorithm-debt@715509306d14c6fac387717c8483eb00425a7e68`。固定上游：`Tencent/WeKnora@1edcd54b43606d9079bb36650efe3f68707a79ea`；现有 Go 二进制 SHA-256 仍为 `491a0bd01577ecff835b0e2c93112e141f550e7bd07fabc523b98f8433e75f6f`，没有重写上游算法。

## V3-00：原件与运行基线

- [SOURCE_INVENTORY.json](SOURCE_INVENTORY.json) 是 **8289 候选副本**的只读盘点：46 个活动版本中，33 个在另一个明确语料目录找到 SHA-256 完全一致的 DOCX，13 个旧模板版本的活动摘要对应派生目录提示，分类为 `DERIVED_ONLY`。这 33 份并未写回候选 Blob；8289 Blob 原件目录仍为空。不能由此推断 18288 的原件状态，也不能把候选目录中的同名文件关联为那 13 个旧版本的原件。
- 新上传模板不再把生成的目录提示 DOCX 作为 DocumentVersion 原件。原始 DOCX 字节先写入 Source Artifact，解析时从相同版本派生仅供检索的模板目录提示。对模板目录的非 DOCX 文件在上传前拒绝，避免产生不可重建的假版本。
- 来源生命周期测试在旧分块模式和父子分块模式下各完成一次：HTTP 上传、Job 成功、原件摘要与 SDK/HTTP 下载逐字节一致、索引不含模板填写正文、重启后从原件重新解析和分块。底层 `LifecycleService._queue_document_version()` 已在入队前写入并回读校验来源 Blob。
- [RUN_MANIFEST.json](RUN_MANIFEST.json) 记录 8289/18288 容器与镜像、活动索引身份及 8289 运行 profile。8289 当前活动 Revision 为 `irev_0329a35ca9ea700133f7b309160118f0`，仍使用 `docx-structural-v3`；18288 活动 Revision 为 `irev_abb7674413b8d095789d6191246310eb`。清单还记录内网模型的安全配置以及 LLM `/v1/models` 报告的 `max_model_len=8192`。服务实际最大输出和模型并发没有得到可靠声明，保持“未实测”；应用 OpenAI 适配器默认输出上限 1536 不能当作服务上限。

## V3-01：阅读域与预览

- 原生 DOCX 的同一正文 story 以连续标题、段落、列表形成输入域；真实 section、story 和表格保留来源边界。旧 `plan_sections` 仍用于页眉、页脚、脚注、批注等独立内容，常规正文不再在每个旧 run 处预切后才调用 Go。
- MD/TXT/docreader Markdown 保留原始解析文本 Artifact；安全阅读文本的节点跨度连续覆盖全文，围栏内的 `#` 不会被当成标题，保留 Markdown 标题层级、列表和表格标记。图片临时地址替换为占位符，记录重复出现的原解析文本坐标和阅读文本坐标；图片与实际素材的完整关联留待 V3-04。
- 换行归一化保存代码点来源映射。Go 接收整阅读域及策略、语言、token 选项，返回的 `selected_tier`、`tier_chain`、`rejected` 和 `profile` 进入预览诊断。长阅读域按有界字符范围分段，子块同时记录局部和全域偏移。片段的脚注与子组仅来自实际覆盖的节点。父级和子级仍绑定同一文档版本，索引指纹随策略和解析身份更新。
- 管理员 `POST /api/v1/admin/wanshitong/candidate/chunk-preview` 使用实际 Parser 和同一 Go Chunker，不创建文档、Job 或活动索引；默认不输出正文。认证、CSRF、候选开关、预览与实际入库结果均有定向回归。

## 已执行验证与边界

- 相关 Python 定向测试 **46 passed**；覆盖直接 Go 包装器差分、Unicode/CRLF、围栏、表格、长域分段、预览对照真实候选索引、模板原件重启重建与 HTTP 下载。修改范围内 Ruff、格式检查、mypy 通过；Go 二进制固定摘要核对通过。
- 本机 WSL 没有 Go 编译器，因此未重新运行上游 Go 单元测试；固定 Go 二进制由上述 Python 差分测试实际调用。前端无本阶段改动，未重跑构建。
- `LIVE_NOT_RUN`：V3 代码没有部署到 8289，也未运行内网真实模型问答或真实语料准确度评估。当前 8289 仍运行原镜像及默认 `legacy` 分块；18288 没有更改。离线通过证明候选代码可执行与入库，不能证明服务器上的候选功能已经启用或回答准确度提升。
- 本阶段未在服务器创建测试容器、镜像、索引或数据目录，因此没有待清理的服务器测试资源。V3-02 到 V3-05 仍未执行；用户页面、端到端流式、图片素材绑定、PDF 和标准发布不属于本次完成范围。

# V3-03H 质量观察

## 同索引旧版／新版对照

两版依次使用 8289 隔离数据、相同的 5 份 DOCX 和 `irev_54dbcdc61a0fc99ad1004a51e6a2f60d`，模型设置未改。问答是随机生成过程，以下只记录这次终态，不把字符数或引用数当作正确率。

| 问题 | 旧版 `9b36652` | 新版 `5d2f723` |
| --- | --- | --- |
| “命名规范中”的来源范围 P0 | `SOURCE_SCOPE_NOT_RESOLVED`，未进入检索 | `ANSWERED`，1 个有效引用，Trace `trace_af5976af751c4f0ca5c217185d7261c0` |
| 原多阶段长问 | `ANSWERED`，1 个引用 | `ANSWERED`，3 个引用 |
| 研发工时长问 | `ANSWERED`，5 个引用 | `ANSWERED`，4 个引用 |
| 名称字数短问 | `ANSWERED`，1 个引用 | `ANSWERED`，1 个引用 |
| 无答案“火星基地”问 | `ANSWERED`，给出无依据审批部门 | `ANSWERED`，仍给出无依据审批部门；Trace `trace_ec6385d0665a46c3a90f23ba7a6af723` |

人工原文核对：`附件4：研发项目命名规范.docx` 写明建议组合为“单位名称（可选）+研发能力领域（可选）+项目内容+动作+项目阶段（可选）”，且名称尽量控制在 30 字以内。普通页面两条对应正式回答与原文一致。未对全部长答案逐项人工复核。

## 未解决问题

1. **有效引用仍可承载错误推断。** 两版均把没有出现在四份业务资料中的“火星基地科研项目”当作一般研发项目，并以一般职责段落推出特定审批结论；请求级 Registry 只校验引用身份，无法证明这一特定事实。详见 [独立 P0](P0-UNSUPPORTED-ANSWER-WITH-VALID-CITATION.md)。
2. **追问可能缺引用。** SSO 页面在名称要素回答后追问“那项目名称建议控制在多少字以内？”，Trace `trace_a4b1ad388548c500c20d9bf6e36a8d19` 已接受查询改写、检索与重排，也送入 5 份材料，模型以 `stop` 结束但没有输出引用，终态 `CITATION_MISSING`，未作为正式答案发布。同样的直接提问在独立会话回答“30 字以内”并带 1 个引用。保留为通用问答质量观察，不添加单题规则。
3. **单纯页面断连不等于停止。** 长问流式显示时刷新页面，随后服务端 Trace `trace_3809f79d8638a84595b1a199f7a33151` 为 `ANSWERED`。显式“停止”已验证为 `CANCELLED`。现有观察不能区分网络断连传播延迟与服务端继续生成，后续如需“断连即取消”，应单独定义并验证该合同。

未运行真实语料总体准确率、压力或 OCR 测试；本轮不据 5 份资料外推生产效果。

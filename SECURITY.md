# 安全说明

## 威胁边界

DOCX、文档文本、OCR 输出、历史问题和检索证据一律视为不可信数据。解析器
限制文件大小、ZIP 条目、解压总量、压缩比和耗时，不执行宏/OLE、不读取
外链。生成链只接受活动索引 manifest 中可逐字核对的引用，历史回答不作为证据。

OCR API 只部署在内部网络，要求独立 Bearer token，并限制原始字节、像素、
并发和超时。容器以非 root、只读文件系统、无额外 capability 运行；模型和
wheels 在镜像构建前校验 SHA256，运行时禁止下载。EMF 在未冻结转换器时明确
失败，不能静默降级为成功。

## 密钥与日志

查询、管理、Qdrant、OCR 令牌不得复用，且只能通过未跟踪的 `.env` 注入。
源码、示例和镜像层不得包含真实凭据。默认日志不记录原文、问题、答案、
Authorization header 或密钥，只记录 trace/job ID、阶段耗时、版本和拒答原因。

## 发布与报告

每次发布必须对 Git index 运行 `scripts/check_release_safety.py`，其私有路径、
内网 IP、本机路径、疑似凭据、大文件和二进制计数都必须为 0。不要在公开
issue 中粘贴私有文档、冻结标签、令牌或服务地址；安全问题应通过仓库所有者
指定的私密渠道报告。项目根目录故意不提供开源许可证。
